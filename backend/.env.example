# Backend server
PORT=3307
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# MySQL connection - fill in with your own non-committed credentials
DB_HOST=localhost
DB_PORT=3307
DB_USER=
DB_PASSWORD=
DB_NAME=Saltenieria
DB_CONNECTION_LIMIT=10

# Authentication - use a long, random value in .env
JWT_SECRET=
JWT_EXPIRES_IN=1h
